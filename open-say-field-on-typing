diff --git a/gameSource/LivingLifePage.cpp b/gameSource/LivingLifePage.cpp
index e16ea248..726531bf 100644
--- a/gameSource/LivingLifePage.cpp
+++ b/gameSource/LivingLifePage.cpp
@@ -176,6 +176,12 @@ static char *photoSig = NULL;
 static double noMoveAge = 0.20;
 
 
+static unsigned int keyDownGap = 0;
+static char sayCapture = 0;
+static int openSayFieldOnTypingLower = 0;
+static int openSayFieldOnTypingUpper = 1;
+
+
 static double emotDuration = 10;
 
 
@@ -2614,6 +2620,9 @@ LivingLifePage::LivingLifePage()
     showShiverSweat = SettingsManager::getIntSetting( "showShiverSweat", 1 );
 
     preferObjectsOverPlayersWithScreenLock = SettingsManager::getIntSetting( "preferObjectsOverPlayersWithScreenLock", 1 );
+
+    openSayFieldOnTypingLower = SettingsManager::getIntSetting( "openSayFieldOnTypingLower", 0 );
+    openSayFieldOnTypingUpper = SettingsManager::getIntSetting( "openSayFieldOnTypingUpper", 1 );
     
     initLiveTriggers();
 
@@ -12129,6 +12138,8 @@ void LivingLifePage::step() {
             mouseHeld = true;
             }
         }
+
+    keyDownGap++;
     
     if( mServerSocket == -1 ) {
         serverSocketConnected = false;
@@ -24172,6 +24183,7 @@ void LivingLifePage::keyDown( unsigned char inASCII ) {
         return;
         }
 
+    bool keyHandledByCase = false;
     
     switch( inASCII ) {
         /*
@@ -24193,6 +24205,7 @@ void LivingLifePage::keyDown( unsigned char inASCII ) {
             if( ! mSayField.isFocused() &&
                 serverSocketConnected &&
                 SettingsManager::getIntSetting( "vogModeOn", 0 ) ) {
+                keyHandledByCase = true;
                 
                 if( ! vogMode ) {
                     sendToServerSocket( (char*)"VOGS 0 0#" );
@@ -24222,6 +24235,7 @@ void LivingLifePage::keyDown( unsigned char inASCII ) {
         case 'I':
             if( ! mSayField.isFocused() &&
                 vogMode ) {
+                keyHandledByCase = true;
                 
                 if( ! vogPickerOn ) {
                     addComponent( &mObjectPicker );
@@ -24238,6 +24252,7 @@ void LivingLifePage::keyDown( unsigned char inASCII ) {
             if( ! mSayField.isFocused() &&
                 serverSocketConnected &&
                 vogMode ) {
+                keyHandledByCase = true;
                 sendToServerSocket( (char*)"VOGP 0 0#" );
                 }
             break;
@@ -24245,12 +24260,14 @@ void LivingLifePage::keyDown( unsigned char inASCII ) {
             if( ! mSayField.isFocused() &&
                 serverSocketConnected &&
                 vogMode ) {
+                keyHandledByCase = true;
                 sendToServerSocket( (char*)"VOGN 0 0#" );
                 }
             break;
         case 'S':
             if( savingSpeechEnabled && 
                 ! mSayField.isFocused() ) {
+                keyHandledByCase = true;
                 savingSpeechColor = true;
                 savingSpeechMask = false;
                 savingSpeech = true;
@@ -24260,6 +24277,7 @@ void LivingLifePage::keyDown( unsigned char inASCII ) {
         case 'X':
             if( userTwinCode != NULL &&
                 ! mStartedLoadingFirstObjectSet ) {
+                keyHandledByCase = true;
                 
                 closeSocket( mServerSocket );
                 mServerSocket = -1;
@@ -24329,18 +24347,21 @@ void LivingLifePage::keyDown( unsigned char inASCII ) {
         */
         case 'e':
         case 'E':
-            if( ! mSayField.isFocused() ) {
+            if( mEKeyEnabled && ! mSayField.isFocused() ) {
+                keyHandledByCase = true;
                 mEKeyDown = true;
                 }
             break;
         case 'z':
         case 'Z':
             if( mUsingSteam && ! mSayField.isFocused() ) {
+                keyHandledByCase = true;
                 mZKeyDown = true;
                 }
             break;
         case ' ':
             if( ! mSayField.isFocused() ) {
+                keyHandledByCase = true;
                 shouldMoveCamera = false;
                 }
             break;
@@ -24655,6 +24676,35 @@ void LivingLifePage::keyDown( unsigned char inASCII ) {
                 TextField::unfocusAll();
                 }
             break;
+        default:
+            keyHandledByCase = false;
+            break;
+        }
+
+    if( keyHandledByCase ) {
+        printf( "key handled by case %c\n", inASCII);
+        sayCapture = inASCII;
+        keyDownGap = 0;
+        }
+    else {
+        printf( "key NOT handled by case %c\n", inASCII);
+        // inspired by Awbz OneLife+
+        if( ( ( openSayFieldOnTypingLower && islower( inASCII ) )
+              || ( openSayFieldOnTypingUpper && isupper( inASCII ) )
+            )
+            && ! mSayField.isFocused()
+            && ! vogMode
+            && (isalpha( inASCII )
+                || (inASCII == ' ' && keyDownGap < 30 && sayCapture != 0 ))) {
+            if( keyDownGap < 30 && sayCapture != 0 ) {
+              mSayField.insertCharacter( sayCapture );
+            } else {
+              sayCapture = inASCII;
+            }
+            mSayField.insertCharacter( inASCII );
+            mSayField.focus();
+            sayCapture = 0;
+            }
         }
     }
 
diff --git a/gameSource/settings/openSayFieldOnTypingLower.ini b/gameSource/settings/openSayFieldOnTypingLower.ini
new file mode 100644
index 00000000..c2270834
--- /dev/null
+++ b/gameSource/settings/openSayFieldOnTypingLower.ini
@@ -0,0 +1 @@
+0
\ No newline at end of file
diff --git a/gameSource/settings/openSayFieldOnTypingUpper.ini b/gameSource/settings/openSayFieldOnTypingUpper.ini
new file mode 100644
index 00000000..56a6051c
--- /dev/null
+++ b/gameSource/settings/openSayFieldOnTypingUpper.ini
@@ -0,0 +1 @@
+1
\ No newline at end of file
