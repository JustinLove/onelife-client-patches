click-through-heads

From: Justin Love <git@JustinLove.name>


---
 gameSource/LivingLifePage.cpp                      |   13 +++++++++++++
 .../preferObjectsOverPlayersWithScreenLock.ini     |    1 +
 2 files changed, 14 insertions(+)
 create mode 100644 gameSource/settings/preferObjectsOverPlayersWithScreenLock.ini

diff --git a/gameSource/LivingLifePage.cpp b/gameSource/LivingLifePage.cpp
index 856e302f..a71beeab 100644
--- a/gameSource/LivingLifePage.cpp
+++ b/gameSource/LivingLifePage.cpp
@@ -126,6 +126,8 @@ static int ignoreTallObjects = 1;
 
 static int showShiverSweat = 1;
 
+static int preferObjectsOverPlayersWithScreenLock = 1;
+
 
 static int screenCenterPlayerOffsetX, screenCenterPlayerOffsetY;
 
@@ -2218,6 +2220,8 @@ LivingLifePage::LivingLifePage()
     ignoreTallObjects = SettingsManager::getIntSetting( "ignoreTallObjects", 1 );
 
     showShiverSweat = SettingsManager::getIntSetting( "showShiverSweat", 1 );
+
+    preferObjectsOverPlayersWithScreenLock = SettingsManager::getIntSetting( "preferObjectsOverPlayersWithScreenLock", 1 );
     
     initLiveTriggers();
 
@@ -19366,6 +19370,15 @@ void LivingLifePage::checkForPointerHit( PointerHitRecord *inRecord,
                     
                     if( dist < minDistThatHits ) {
                         p->hit = true;
+                        if( preferObjectsOverPlayersWithScreenLock &&
+                            !shouldMoveCamera &&
+                            p->hitOurPlacement ) {
+                            if( p->closestCellY > y ) {
+                                p->hitOurPlacementBehind = true;
+                                }
+                            continue;
+                            }
+
                         p->closestCellX = x;
                         p->closestCellY = y;
                         if( o == ourLiveObject ) {
diff --git a/gameSource/settings/preferObjectsOverPlayersWithScreenLock.ini b/gameSource/settings/preferObjectsOverPlayersWithScreenLock.ini
new file mode 100644
index 00000000..d00491fd
--- /dev/null
+++ b/gameSource/settings/preferObjectsOverPlayersWithScreenLock.ini
@@ -0,0 +1 @@
+1
